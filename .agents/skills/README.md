# Repository skills

`img2threejs` is checked out as a Git submodule. Initialize repository skills
after cloning with `git submodule update --init --recursive`.
The review web app consumes unified furniture packages from
`lib/bedroom/assets/builtin` and `lib/bedroom/assets/user-generated`; it does not
execute the skill itself.
