# Repository skills

`img2threejs` is intentionally linked from a local checkout instead of copied
into this repository. Run `scripts/link-img2threejs-skill.ps1` after cloning.
The review web app consumes generated TypeScript assets; it does not execute the
skill itself.
